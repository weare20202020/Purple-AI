# 1.7 — 2026-06-02 修改记录

## 原生 Function Calling 迁移

将自定义 JSON ReAct 解析替换为各平台原生 function calling：

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/llm/types.ts` | 修改 | 新增 `LLMResponse`、`ToolCall`、`ToolDef`；`content: string \| null` |
| `src/llm/zod-to-json.ts` | 新建 | ZodObject/Optional/Enum/Array→JSON Schema，兼容 Zod v4 |
| `src/llm/openai.ts` | 重写 | 发送 `tools[]`，解析 `response.tool_calls` |
| `src/llm/deepseek.ts` | 重写 | 同 OpenAI 格式 |
| `src/llm/anthropic.ts` | 新建 | 转换 `tool_use`/`tool_result` 协议，`system` 放顶层 |
| `src/llm/structured.ts` | 删除 | 不再需要自定义 JSON 格式 |
| `src/agent/runtime.ts` | 重写 | ReAct 循环：`callLLM()` 返回 `LLMResponse`，`addToHistory` 支持原生 `tool_calls`/`tool_call_id`，系统提示删掉 JSON 格式说明 |
| `src/agent/manager.ts` | 修改 | 注册 `AnthropicProvider` |

## Bug 修复

| # | Bug | 位置 | 修复 |
|---|-----|------|------|
| B1 | 旧 session 的 `tool` 消息缺少 `tool_call_id` → DeepSeek 400 | `runtime.ts:31,712,631` | `repairHistory()` 加载时修复 + `callLLM` 运行时兜底 |
| B2 | `system`→`assistant` 转换被打散的 assistant/tool 对 | `runtime.ts:435` | map 中统一处理 |
| B3 | Zod v4 不兼容（`_def.values`→`_def.entries`，`_def.type`→`_def.element`） | `zod-to-json.ts:16,40` | 修正内部属性访问 |
| B4 | TypeScript 类型错误 ×9 | `runtime.ts`、`zod-to-json.ts` | null content、LLMResponse 返回类型、隐式 any 等 |
| B5 | Anthropic `JSON.parse(tc.arguments)` 无 try/catch | `anthropic.ts:62` | 加 try/catch，失败 fallback `{ _raw: tc.arguments }` |
| B6 | 内存压缩切分点可能拆散 `assistant(tool_calls)`/`tool` 对 | `runtime.ts:494` | `splitIdx` 跳过孤立 tool 消息 |
| B7 | `processIncomingMessage` 直接 push 绕过 `addToHistory` | `runtime.ts:256` | `push` → `addToHistory`，token 计数和 100 条上限都正常走 |
| B8 | `newSession` 未 await `saveSession()` | `runtime.ts:590` | 加 `await` |
| B9 | `addToHistory.shift()` 制造孤儿 tool 消息 | `runtime.ts:479` | shift 后检查首条是否为 `tool`，是则继续移除 |
| B10 | `send_message` 返回值被硬编码 `'ok'` 吞掉 | `runtime.ts:196,294,756` | `executeSkill` 返回 `Promise<string>`，capture 实际结果 |
| B11 | `processIncomingMessage` max steps 分支缺 `saveSession()` | `runtime.ts:336` | 补 `await this.saveSession()` |
| B12 | `estimateTokens` 只计 `content`，忽略 `tool_calls` | `runtime.ts` → 文件级函数 | 新增 `estimateEntryTokens()` 同时计 name+arguments |

## 关键设计决策

- `send_message` 注册为 skill，但作为 tool 暴露给 LLM，在 ReAct 循环中路由到 `executeSkill`
- 所有 LLM provider 的 `generate()` 统一返回 `LLMResponse { content, toolCalls? }`
- 旧 session 加载时自动 `repairHistory()` 修复数据格式不兼容

---

## 差距分析（vs FIgurev1-2.md）

### ✅ 已实现

| 需求 | 实现位置 |
|------|----------|
| 独立 Agent Profile | `types.ts:13` |
| 纯消息驱动协作 | `send-message.ts` + `runtime.ts processIncomingMessage` |
| 同一 Session 连续性 | `loadSession` / `saveSession` / `archiveCurrentSession` |
| 用户输入优先级 | `processingInbox` lock |
| 按需启动 Agent | `manager.ts startAgent` |
| 异步工作流（不阻塞） | 系统提示 rule 7 + send_message skill |
| HTTP `/receive` | `MessageServer` + `handleIncoming` |
| Inbox 持久化 | `Inbox` 类 + `inbox.json` |
| 消息来源区分 | `[From Name (id) via team message]` |
| 持久记忆 | `experience.ts` + `page-index.ts` |
| LLM 多后端 | OpenAI / DeepSeek / Anthropic |
| CLI 框架 | Ink (React-based) |
| 消息类型标准 | `types.ts Message` |

### ⚠️ 部分实现

| 需求 | 差距 |
|------|------|
| `context_summary` | 类型有字段，`send_message` 接收并转发，但 `processIncomingMessage` 丢弃，接收方 Agent 看不到上下文 |
| `task_id` / `reply_to` | 类型有字段，但 `send_message` 不设置，无法追踪任务链路 |
| 消息类型 `question` / `summary` | 类型定义包含，但 `send_message` schema 只暴露 `task` / `result` / `broadcast` |
| Inbox 消息合并 | 有队列但无合并逻辑，多条消息依次单独处理 |

### ❌ 未实现

| 需求 | 说明 |
|------|------|
| 动态角色与权限 | 配置静态，改角色需重启 |
| 共享 MD 文档 | 文档提到"可选共享 MD 用于记录结果节约 token"，未实现自动化读写 |
| Output Lock + "正在处理…" 提示 | 用户输入时 queue 不处理，但无 UI 提示 |
| 打包为单文件 .exe | 无打包脚本 |
| `task_id` / `reply_to` 链路追踪 | 字段存在但 send_message 不使用 |
| 权限系统 | 完全缺失 |
