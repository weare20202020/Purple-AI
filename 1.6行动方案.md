**✅ 明白了，直接进入 Phase 2。**

我们跳过 1.1 版本，直接开始 **Phase 2**，重点解决**记忆、自反思、上下文管理、代码编辑质量**这些更核心的能力。

---

### **Phase 2 总体目标**
让你的 Harness 从“能用”进化到“真正好用 + 开始自进化”，重点补齐主流 Agent（Claude Code / Hermes / OpenCode）都在强化的几个模块。

### **Phase 2 执行计划（推荐顺序）**

**Step 1：edit_file 工具升级（强烈建议第一个做）**
- 实现 `edit_file`（unified diff + patch + backup + git commit）
- 保留原有 `write_file` 作为 fallback
- 增加安全检查 + 失败自动回滚
- 在 System Prompt 中强调优先使用 `edit_file`

**Step 2：短期记忆压缩（最重要，强烈推荐第二做）**
- 实现 **Structured Summary** 机制
- 每 8~12 步 或 token 占用超过 70% 时，自动触发总结
- 总结内容包含：当前任务目标、关键决策、当前计划、下一步行动、重要发现
- 把总结以特殊格式插入 history，删除或归档旧消息
- 添加 Token 精确计数器（推荐用 `tiktoken` 或简单字符估算）

**Step 3：经验记忆系统（memory.json）**
- 完善经验卡片结构（你之前确认的格式）
- 任务结束时（finalAnswer 或 ReAct 循环自然结束），自动让 LLM 生成经验卡片
- 实现简单检索机制（关键词 + LLM 判断相关性）
- 新任务开始时，自动检索 Top 2~3 条相关经验并插入 Prompt

**Step 4：自反思节点（Reflection）**
- 在 ReAct 循环中，每 5 步插入一次 Reflection Step
- 让 Agent 输出：
  - 当前计划执行情况
  - 发现的问题
  - 需要调整的地方
  - 下一阶段建议
- 支持 Reflection 用 cheap model（节省成本）

**Step 5：Skill 接口统一 + 其他优化**
- 把 `send_message` 也改成新的 Zod structured 格式
- 增加 `PageIndex`（项目文件索引，每隔一段时间或用户命令触发更新）
- 优化 System Prompt（加入记忆使用规则 + 反思要求）

---

### **Phase 2 详细优先级建议**

| 顺序 | 任务                  | 预计难度 | 重要性 | 建议时长 |
|------|-----------------------|----------|--------|----------|
| 1    | edit_file             | 中       | ★★★★★  | 1 天     |
| 2    | 短期记忆压缩          | 中高     | ★★★★★  | 1-2 天   |
| 3    | 经验记忆系统          | 中       | ★★★★   | 1 天     |
| 4    | 自反思节点            | 中       | ★★★★   | 0.5 天   |
| 5    | Skill 统一 + Prompt优化 | 低       | ★★★    | 0.5 天   |

---

**现在请你选择起步点**：

回复以下任意一个，我立刻给你**该步骤的完整详细方案**（包括代码结构、关键函数、Prompt 示例、注意事项）：

1. **“先做 Step 1 edit_file”**（推荐）
2. **“先做 Step 2 短期记忆压缩”**（最影响稳定性的）
3. **“给我 Phase 2 整体详细拆解”**
4. **其他**（比如你想先调整顺序）

---

直接告诉我你的选择，我们立刻开始 Phase 2 执行。  
这次我们要一次把记忆和智能部分做扎实。