最终目标架构（推荐最终形态）发送消息：HTTP POST（保持简单可靠）
接收消息：HTTP Server 直接触发处理（彻底消灭 setInterval）
持久化：仍然保留 inbox.json（用于重启后未处理消息恢复）
返回结果：HTTP POST + 可选 SSE（后期可平滑升级）
消息队列：继续使用你现有的 PriorityQueue，只是触发方式从轮询改成事件

彻底重构步骤（建议分两天完成）Day 1：核心改造（去掉所有轮询，变成真事件驱动）1. 修改 src/network/server.ts（最重要）ts

// server.ts
export class MessageServer {
  private runtime: AgentRuntime;

  constructor(runtime: AgentRuntime) {
    this.runtime = runtime;
  }

  start(port: number) {
    // ... 原有 express/app 创建代码

    app.post('/receive', async (req, res) => {
      const message: Message = req.body;
      
      try {
        // === 关键改动：直接处理，不再只写文件 ===
        await this.runtime.processIncomingMessageImmediately(message);
        
        // 仍然持久化（用于重启恢复）
        await this.runtime.inbox.add(message);
        
        res.json({ status: 'ok', messageId: message.id });
      } catch (err) {
        console.error('[Server] 处理消息失败', err);
        res.status(500).json({ status: 'error' });
      }
    });

    app.listen(port, () => {
      console.log(`[Agent ${this.runtime.id}] 事件驱动服务器已启动，端口 ${port}`);
    });
  }
}

2. 在 AgentRuntime 中新增方法（runtime.ts）ts

// 新增这个方法，专门给 Server 直接调用
async processIncomingMessageImmediately(message: Message) {
  // 立即加入优先级队列
  this.inbox.add(message);           // 你的 PriorityQueue
  
  // 立即处理（不再等轮询）
  await this.processNextMessage();
}

// 原有的 processIncomingMessage 改名或调整
async processNextMessage() {
  const message = this.inbox.getNext(); // 从队列取最高优先级
  if (!message) return;
  
  // ... 你原来的处理逻辑：addToHistory、callLLM、执行 skill 等
}

3. 彻底删除所有 setInterval 轮询代码搜索整个项目，删除所有 setInterval 中检查 inbox 的逻辑。
只在 AgentRuntime.start() 中启动 HTTP Server 即可。

4. 发送端小优化（client.ts）
发送后可以加简单重试 + timeout，但不需要大改。Day 2：进一步完善（让它更专业）消息确认机制（防止丢失）接收方处理完后返回 {status: 'processed', messageId} 
发送方收到确认后再标记已发送

Agent Card（能力声明）每个 Agent 启动时，向其他 Agent 声明自己有哪些 Tool/Skill
方便 Alice 知道 Bob 能干什么

错误重试与死信队列处理失败的消息进入 dead-letter inbox

可选升级到 SSE（实时推送）后期可以轻松加上 /events 端点，实现真双向推送。
