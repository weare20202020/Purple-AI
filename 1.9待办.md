# 1.9 待办

## Agent 创建 Agent
- 注册 `create_agent` 工具，让 Agent 能动态创建新 Agent
- 加审批流程：Agent 调用后挂起，用户输入 `/approve` 确认后才真正创建
- 需要新建 `src/tools/create-agent.ts`，修改 `runtime.ts`（注册 + pendingCreation）、`classic.ts` / `app.tsx`（加 `/approve` 命令）

## 端口冲突 & 多实例支持
- 目前每个 Agent 独占一个端口（A=3000, B=3001...），无法多开
- 三个可选方案：
  1. 启动时检测端口被占，自动偏移 + 运行时覆盖 config
  2. 砍掉 HTTP server，纯进程内通信（EventEmitter / 回调链），任意多开
  3. 保留 HTTP，加 instance-scoped 端口池 + 自动发现
- 方案 2 最实用，本地场景不需要走 HTTP，省序列化开销

## 部署优化
- `package.json` 加 `"postinstall": "tsc"`，clone 后 `npm install` 自动 build
- 后续用 `purple` 直接跑，不再需要手动 `npm run build`

## Ink 终端（purple）spinner 动画
- 当前 spinner 只在 workingIds 变化时才更新帧，不转
- Phase 2 改成事件驱动，定时 commit
